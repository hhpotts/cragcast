name: Deploy CragCast to Cloudflare

on:
  push:
    branches:
      - main
      - staging
    paths:
      - "assets/**"
      - "components/**"
      - "composables/**"
      - "pages/**"
      - "plugins/**"
      - "public/**"
      - "server/**"
      - "utils/**"
      - "types/**"
      - "worker-cron/**"
      - "app.vue"
      - "app.config.ts"
      - "nuxt.config.*"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/deploy-cloudflare.yml"
  workflow_dispatch:

jobs:
  deploy-pages:
    name: Deploy Pages App
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Enable corepack (use pnpm from package.json)
        run: corepack enable

      - name: Show pnpm version
        run: pnpm --version

      - name: Determine pnpm store path
        run: echo "PNPM_STORE_PATH=$(pnpm store path)" >> $GITHUB_ENV

      - name: Ensure PNPM store dir exists
        run: mkdir -p "$PNPM_STORE_PATH"

      - name: Setup PNPM cache
        uses: actions/cache@v4
        with:
          path: ${{ env.PNPM_STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: pnpm fetch
        run: pnpm fetch

      - name: Install dependencies (frozen, offline if possible)
        run: pnpm install --frozen-lockfile --prefer-offline

      # Build for Cloudflare Pages (Nitro cloudflare-pages preset)
      - name: Nuxt build
        env:
          NITRO_PRESET: cloudflare-pages
        run: pnpm run build

      # Deploy to Cloudflare Pages (SSR via Pages Functions generated by Nitro)
      - name: Deploy Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4'
          command: pages deploy dist --project-name=cragcast --branch=main
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

  deploy-staging:
    name: Deploy Staging
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Enable corepack (use pnpm from package.json)
        run: corepack enable

      - name: Determine pnpm store path
        run: echo "PNPM_STORE_PATH=$(pnpm store path)" >> $GITHUB_ENV

      - name: Ensure PNPM store dir exists
        run: mkdir -p "$PNPM_STORE_PATH"

      - name: Setup PNPM cache
        uses: actions/cache@v4
        with:
          path: ${{ env.PNPM_STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: pnpm fetch
        run: pnpm fetch

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Nuxt build
        env:
          NITRO_PRESET: cloudflare-pages
        run: pnpm run build

      - name: Deploy to Staging
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4'
          command: pages deploy dist --project-name=cragcast-staging --branch=main
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

  deploy-worker:
    name: Deploy Cache Warmer Worker
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # worker-cron is a separate npm package (not part of the root pnpm
      # workspace) with its own lockfile — installing here means wrangler is
      # already present when wrangler-action runs, so it never has to guess
      # a package manager itself. That auto-detection is what was failing:
      # https://github.com/cloudflare/wrangler-action/issues/338
      - name: Install worker-cron dependencies
        working-directory: worker-cron
        run: npm ci

      - name: Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4'
          workingDirectory: worker-cron
          command: deploy
